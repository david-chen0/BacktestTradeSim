#include <iostream>

int main(int argc, char* argv[]) {
	// Workflow:

	// Initialize some starting variables here, such as starting date, end date, starting capital, list of securities to consider
	// just make these hardcoded for now, can accept arguments later on
	double startingCapital = 1000000; // Starting capital of 1 million

	// Initialize the strategy, whatever's needed there
	// Create the portfolio and start running the strategy using the portfolio

	// Plot/return the results somehow

	return 0;
}